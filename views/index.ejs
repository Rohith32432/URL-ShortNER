<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Shortify</title>
    <link rel="icon"
        href="https://png.pngtree.com/element_our/20190528/ourmid/pngtree-electronic-link-tool-image_1131799.jpg">
    <style>
        .urls::-webkit-scrollbar {
            background-color: rgb(18, 18, 18);
            padding: 2px;
            width: 5px;
        }

        ::-webkit-scrollbar-thumb {
            background-color: rgb(59, 59, 59);
            border-radius: 5px;
            margin: 5px;
        }
    </style>
</head>

<body class="d-flex flex-column justify-content-center align-items-center vh-100 text-white bg-black m-0">
    <div class="bg-dark rounded-3 p-4 w-75" style="max-width: 800px;">
        <div class="text-center d-flex flex-column align-items-center">
            <h1 class="text-light">URL Shortener</h1>
            <form action="/posturl" method="POST" class="my-4 w-100 d-flex flex-row  align-items-center gap-2">
                <input required placeholder="Enter URL" type="url" name="fullUrl" id="fullUrl" class="form-control w-100">
                <button class="btn btn-success px-4 " type="submit">Shrink</button>
            </form>
        </div>
        <div class="urls rounded-3 p-3 overflow-auto" style="height: 400px; background-color: rgb(18, 18, 18);">
            <% shortUrls.forEach(shortUrl=> { %>
            <div class="d-flex flex-column flex-md-row align-items-md-center bg-dark text-light p-3 mb-3 rounded gap-3">
                <img src="https://www.google.com/s2/favicons?domain=<%= shortUrl.fullurl %>&sz=64" alt=""
                    class="img-fluid rounded-circle mx-auto mx-md-0" style="height: 50px; width: 50px;">
                <div class="overflow-hidden text-center text-md-start">
                    <a href="<%= shortUrl.shorturl %>" class="d-block text-info text-decoration-none">
                        <%= shortUrl.shorturl %>
                    </a>
                    <a href="<%= shortUrl.fullurl %>" class="d-block text-light text-decoration-none">
                        <%= shortUrl.fullurl.substring(0, 50) %>
                    </a>
                </div>
                <a href="/del/<%= shortUrl.shorturl %>" class="mt-2 mt-md-0 ms-auto">
                    <button class="btn btn-danger btn-sm w-100 w-md-auto">Delete</button>
                </a>
            </div>
            <% }) %>
        </div>
    </div>
</body>

</html>
